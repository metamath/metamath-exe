*.o
*.Po
autom4te.cache/
stamp-h1
missing
metamath
metamath_test
Makefile
Makefile.in
compile
configure
configure~
config.*
depcomp
install-sh
aclocal.m4
autoscan.log
configure.ac2
*~1
*~2
*~3
*~4
*~5
*~6
*~7
*~8
*~9
build/
# Cosmopolitan toolchain, installed locally for cross-platform builds (not checked in)
cosmocc/
# build-cosmo/, build-test/, build-cosmo-test/: one directory per set of
# compiler flags, so that no two builds ever reuse each other's objects.
# Only directories, so build-wasm.sh is not matched.
build-*/
# Emscripten SDK and WebAssembly build output (not checked in)
emsdk/
wasm-dist/
# Doxygen related: configuration file and destination folderbuild/

Doxyfile
doxy/
*.produced

# Working directory of tests/smoke_databases.sh
,smoke/

# Artifact written by the tests/untab test
tests/untab-rewrapped.mm

# Artifacts written by the tests/proof-section test
tests/proof-section-out.mm
tests/proof-section-plain.mm
